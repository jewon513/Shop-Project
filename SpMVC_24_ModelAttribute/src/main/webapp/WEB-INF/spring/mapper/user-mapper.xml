<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.biz.model.dao.UserDao">

	<insert id="insert" parameterType="userVO"> 

		INSERT INTO  `books` . `tbl_user` 
			( userId ,
			 password ,
			 userName ,
			 userRole )
		VALUES
			(#{userId} ,
			#{password} ,
			#{userName} ,
			#{userRole} )

	</insert>

</mapper> 